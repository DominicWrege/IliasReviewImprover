{
	"name": "ilias-review-improver",
	"version": "1.3.3",
	"description": "Simple Ilias Review Improver",
	"main": "main.ts",
	"author": "Dominic Wrege",
	"license": "MIT",
	"dependencies": {
		"jszip": "^3.10.1"
	},
	"devDependencies": {
		"web-ext": "^7.8.0"
	},
	"scripts": {
		"start": "bun build ./src/main.ts --minify --outfile=./dist/main.bundle.js && web-ext run -c config.js",
		"build": "bun build ./src/main.ts --minify --outfile=./dist/main.bundle.js && web-ext lint -c config.js && web-ext build -c config.js -o",
		"lint": "web-ext lint -c config.js",
		"docs": "web-ext doc",
		"watch": "bun build ./src/main.ts --outdir ./dist --watch"
	},
	"packageManager": "bun",
	"webExt": {
		"artifactsDir": "build"
	}
}
